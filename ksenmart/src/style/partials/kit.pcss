.kit {
    margin-bottom: 8.5rem;

    @media (--mobile-sm) {
        margin-bottom: 3.5rem;
    }

    &-price{

        margin-bottom: .9rem;

        &__text {
            font-size: 1.3rem;
            line-height: 1.7rem;
            color: #7C7C7C;

            margin-bottom: 0.4rem;
        }
    }

    &-saving {
        margin-bottom: 2.9rem;

        @media (--mobile-sm) {
            margin-bottom: .5rem;
        }
    }
}